<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.intellif.intellif">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.PicDetectActivity" />
        <activity android:name=".main.VideoDetectActvity" />
        <activity android:name=".main.PicCompareActivity" />
        <activity android:name=".main.VideoCompareActivity" />
        <activity android:name=".main.DBActivity" />
        <activity android:name=".main.VideoSearchActivity" />
        <activity android:name=".main.FaceAttrActivity" />
        <activity android:name=".main.LivingDetectActivity" />
        <activity android:name=".main.InfraredActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".main.TestActivity" />

    </application>

</manifest>